#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @n: is the integer
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
